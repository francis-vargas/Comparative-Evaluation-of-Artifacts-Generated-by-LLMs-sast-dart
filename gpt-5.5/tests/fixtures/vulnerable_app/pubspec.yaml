name: vulnerable_app
description: Intentionally vulnerable Flutter sample for dart_sast tests.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_webview_plugin: ^0.4.0
